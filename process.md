
OP 1
extraction of str creates
EU_Full-audio_none-notRenamed/
it contains only the files extracted from the archives, lacks the og audio and video files
lacks flattened tree structure
lacks renamed base folders
lacks reorganized audio files

OP 2
extract textures from txd files inside EU_Full-audio_none-notRenamed/**
this makes no change to the folder name
it simple produces .dds files in a folder named after the source txd file, inside the same folder as the source txd file

OP 3
convert dds to png
simply adds png files next to the dds files created in OP 2

OP 4
extract dialogue/subtitles from lh2 files, convert to csv
creates a csv file for each lh2 file, with the same name, in the same folder as the source lh2 file

OP 5
convert .preinstanced 3d models into .blend and .glb
this makes no change to folder name and simple adds two addional files for each source .preinstanced file, with the same name but different extension, in the same folder as the source .preinstanced file

OP 6
convert videos from .vp6 to .ogv
this makes no change to folder name and simply adds an .ogv file for each source .vp6 file, with the same name, in the same folder as the source .vp6 file

OP 7
convert audio from .snu to .wav
this makes no change to folder name and simply adds a .wav file for each source .snu file, with the same name, in the same folder as the source .snu file

OP 8
reorganize audio files into language specific folders, and global for non language specific files
this creates an EN folder for all audio files that are in the "english" folder in the source, and moves them there
this creates a Global folder for all audio files that are in the "ambiant" folder in the source, and moves them there
it renames the main folder from EU_Full-audio_none-notRenamed/ to EU_Full-audio_reorg-notRenamed/ to reflect the change
and updates the config to reflect this

OP 9
rename base folders for clarity
this renames the base folders within EU_Full-audio_reorg-notRenamed/ to be more clear, for example "audiostreams" becomes "A1-Audio", "movies" becomes "A1-Video"
updates the config to reflect the change
and renames the main folder from EU_Full-audio_reorg-notRenamed/ to EU_Full-audio_reorg-isRenamed/ to reflect the change

OP 10
normalize folder structure
this flattens the folder structure to remove unnecessary nesting
this updates the config to reflect the change, does not change the main folder name, as it produces a new folder named EU_FullFlattened-audio_reorg-isRenamed/ and copies the
files into the flattened structure within that folder
this process also introduces the ever important reproducable id numbers assinged to every file that will always be the same every time
it also outputs a json map of every file with old path, new path, and id, essentially an index/manifest of the game files
the id's are created based on path and name of the file, so they are consistent and reproducable, as long as steps before this dont change the paths or file names

OP 11
generate godot game (EXPERIMENTAL)






problems
the blender process generates an index of the preinstanced files and their corresponding .blend and .glb files, but this uses the old structure and contains no id's
the flattening process is run near the end meaning ids are not assigned until the end, so blender cannot take advantage of the ids
the godot process currently relies on hardcoded relative paths for .glb and .png assets, this will break if any aspect of the folder structure changes, including the flattening process, and the renaming of base folders, and the reorganization of audio files etc, so it needs to rely on the id's generated by the flattener or another tool that generates the same id's


i need an index/manifest that can deterministicly create a cononical id for each file at every step of the process, so that the blender and godot processes can rely on these ids to find the corresponding files they need, regardless of any changes to folder structure or file names that may occur in the earlier steps
so it needs to know the base folder new and old names so that it can generate the same id for the same file
it needs to beable to predict the flattened paths of the files based on the original paths and the operations performed on them, so that it can generate the same id for the same file before and after flattening,
many files dont exist until later stages of the process, for example the .blend and .glb files dont exist until the blender process, currently blender is already predicting the .blend and .glb file paths based on the original .preinstanced file paths, so it needs to be able to generate the same predicted paths with ids for these files

the blender process is dependent on the texture extraction process to extract the textures from the txd files, and the texture conversion process to convert them to png

perhaps a better order of operations could be used

the audio files are aleady id'd and re-organised by another op so it can occur at any time





